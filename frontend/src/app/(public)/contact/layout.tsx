import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact | Prof. (Dr.) Bhagwan Singh",
  description: "Get in touch with Prof. (Dr.) Bhagwan Singh for research collaboration, academic inquiries, or consulting.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
